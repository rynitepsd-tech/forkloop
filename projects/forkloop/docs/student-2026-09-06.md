# Student session ledger — 2026-09-06 (fair conventions for base Fara 1.5 4B on family 3)

**Status: complete. Six collects, $1.67 of Solari; every number below is measured.**

Goal: find out whether the base student (`microsoft/Fara1.5-4B`, served on this Mac) can be made to reach the task at
all with fair, convention-level changes (nothing task-specific, no coordinates, no answers), then give the honest base
number for the SFT ladder. Teacher work frozen. Budget: $5 Solari, $20 OpenAI (unused), $0 Anthropic.

## Preflight (offline unless stated)

| check | result |
| --- | --- |
| `rm -rf venv && python3.11 -m venv venv && pip install -e ".[dev,world,teacher]"` | ok |
| `./venv/bin/pytest -q` | 205 passed before this session's code, **210 passed** after it |
| `forkloop reap --dry-run` | 0 forkloop machines listed |
| power | AC power, 100 % (`pmset -g batt`) |
| model server | mlx-vlm 0.6.17, `microsoft/Fara1.5-4B` bf16 on port 8001 under `caffeinate -i`, `--max-num-seqs 2` |
| click check (`scripts/student_click_check.py --seed 200`, one fork, `runs/logs/click-check-s200-0906`) | model point (315, 195) in the 1000×1000 space → screen (403, 140), crosshair on the *Patients* nav link (hand-checked); 1.76 s per call; reset 32.2 s |

## Step 1 — three 5-seed probes (resolve_denial seeds 200–204, `--nav-macro --max-steps 60 --max-seconds 600`, concurrency 2)

Every probe is a policy-side option recorded in `run.json` under `policy_options`; the world, the generator and the
task manifests are unchanged (commit bf8196a). Scored on one thing: episodes whose OpenEMR `log` holds a successful
`login` row for `admin` after the seed watermark (`verdict.details.ui_milestones.rungs.openemr_login`, step 2's
detector). Gate: the cheapest combination that logs in on 3/5 or better.

### Probe (a) — `--instruction-note` (`runs/fara15-4b-probe-a-note-f3-s200-204`, 20:04–20:20 local)

Note appended to every instruction the model sees: *Credentials for OpenEMR: username admin, password pass. Click the
Username field, type admin, click the Password field, type pass, click Login.*

| metric | value |
| --- | --- |
| **OpenEMR login (the gate)** | **5/5** — successful `login` rows: 1, 2, 2, 2, 1 per episode; failed logins 12, 1, 5, 2, 9 |
| success | 0/5 = 0 % [0, 43.5]; milestones 0.000 |
| invalid-action rate | 0.0 % (0/304) |
| end reasons | step budget (60) ×5; no `ask_user_question` |
| classes (`classify_failures.py`) | budget-sane 2, budget-looping 3 |
| median steps / wall / reset | 61 / 256 s / 32 s |
| $ VM (`forkloop metrics`) | **$0.047**; 732 k tokens in / 27 k out, priced at $0 |

**Reading:** the two-field login is a *reading* failure, not a motor one. Told which word is the password, base Fara
clicks Username, types `admin`, clicks (or Tabs to) Password, types `pass`, clicks Login, and OpenEMR accepts it in
every episode (seed 200 step 9, seed 201 step 9, seed 203 step 48 …). What kills the episodes now sits one screen
later:

1. **Chrome's renderer crash on the post-login page.** In 3/5 episodes the first authenticated page
   (`interface/main/tabs/main.php`) is "Aw, Snap! Error code: 5" with a Reload button (crashpad reports per episode
   4, 4, 4, 2, 0; the crash class already known from the teacher runs; Chrome runs with `--disable-gpu`). The model clicks Reload (correct), lands on the login page
   again (the session is gone with the crashed renderer), logs in again, and crashes again: 12 failed + 1 successful
   login on seed 200 are this loop with the two-field sequence drifting (a `pass` typed into Username once the fields
   are empty).
2. **The note is read as a checklist.** Seed 203 reaches the calendar at step 50 ("the login likely succeeded"),
   then clicks *Flow*, decides "the task explicitly instructs me to log in", navigates to `/openemr/login.php` (404)
   and back to the login page. Seed 201 reaches the calendar with an *Add event* modal open (its click on the
   calendar) and takes the modal for the login form. Neither knows what to do in OpenEMR after logging in; nothing
   in the instruction says "search the patient, open Documents".

Staircase (`scripts/milestone_staircase.py`): login_page 5/5, openemr_login 5/5, openemr_chart 0/5, document 0/5,
auth_typed 0/5, portal_claim 0/5, appeal_form 0/5, appeal_submitted 0/5.

### Probe (b) — `--system-prompt-file forkloop/policies/prompts/fara_no_user_v1.md` (`runs/fara15-4b-probe-b-prompt-f3-s200-204`, 20:21–20:32 local)

The prompt keeps Fara's identity and `computer_use` schema (`{fara_identity}` / `{fara_tools}` placeholders; the
nav-macro enum is still advertised), replaces the critical-points text with "there is no user; the task text is
complete; never use ask_user_question", and appends the v5 world conventions (one tab per app, F5 on a broken page
and log in again, search by surname, read documents fully, the portal appeal route, copy codes exactly, keep the
number in your reasoning). Nothing task-specific, no coordinates, no answers. The instruction is the generator's own.

| metric | value |
| --- | --- |
| **OpenEMR login (the gate)** | **4/5** — successful logins 1, 1, 1, 0, 1; failed logins 4, 0, 5, 5, 2 |
| `ask_user_question` stops | **0/5** (was 10/30 with the trained prompt) |
| success | 0/5 = 0 % [0, 43.5]; milestones 0.000 |
| invalid-action rate | 0.0 % (0/300) |
| end reasons | step budget (60) ×5 |
| classes | budget-sane 3, budget-looping 2 |
| median steps / wall / reset | 60 / 206 s / 69 s |
| $ VM | **$0.039**; 705 k tokens in / 19 k out |

**Reading:** without being told which word is the password the model still types `admin` into the Password field
first (seed 200 steps 9–16: `admin`, ctrl+a, `admin`, Return, `admin`, Return → "Invalid username or password"), but
the prompt's "every field is separate: click the next field before typing" plus the loop warning get 4/5 to a
successful login within 10–26 steps (seed 201 logged in on the first attempt at step 10). After the login the model
now *has a plan*: seed 201 opens the Patient menu, the search page, types `Silas` then `Okafor` (first/last), scrolls the
list; seed 200 reaches the Patient Search modal and its "Search by" dropdown at step 57; seed 202 is the only one that
went to the portal claim list (5 page views). None reached the patient chart (`openemr_chart` 0/5): the search
attempts type the first name into a surname field, Chrome's post-login crash sends 2/5 back to the login page, and
60 steps run out. No episode stopped itself.

Staircase: login_page 5/5, openemr_login 4/5, openemr_chart 0/5, document 0/5, auth_typed 0/5, portal_claim 0/5,
appeal_form 0/5, appeal_submitted 0/5.

### Probe (c) — `--history-notes` (`runs/fara15-4b-probe-c-notes-f3-s200-204`, 20:32–20:51 local)

The trained Fara prompt and the generator's instruction; the model's own reasoning line is shown next to each of the
last 8 actions.

| metric | value |
| --- | --- |
| **OpenEMR login (the gate)** | **1/5** — successful logins 0, 0, 2, 0, 0; failed logins 13, 2, 0, 6, 25 |
| `ask_user_question` stops | 0/5 |
| success | 0/5; milestones 0.000; invalid 0.0 % (0/301) |
| end reasons | step budget (60) ×5; classes budget-sane 2, budget-looping 3 |
| median steps / wall / reset | 60 / 254 s / 69 s |
| $ VM | **$0.047**; 856 k tokens in / 30 k out |

**Reading:** the notes do not tell the model which word is the password, and they make the retry worse: seed 200
types `pass`, `pass`, `pass` into the same field ("the previous note says the password was entered"), seed 203 six
times, seed 204 guesses `password` and then presses Login 25 times. Seed 202 logged in twice (the two-field sequence
happened to come out right) and then re-navigated to `/openemr` and logged in again. Gate failed, so the combined
probe below leaves the notes out (they are also the one option that costs tokens per step: 856 k in vs 705–732 k).

### Combined probe (a)+(b) — note + no-user prompt (`runs/fara15-4b-probe-ab-f3-s200-204`, 20:51–21:07 local)

(c) failed the gate, so the combined probe is the union of the two options that passed it (the chain script picked it
automatically: `runs/logs/probe_chain_abc.sh`).

| metric | value |
| --- | --- |
| **OpenEMR login (the gate)** | **5/5** — successful logins 1, 1, 1, 1, 1; failed logins 0, 2, 2, 6, 0 |
| `ask_user_question` stops | 0/5 |
| success | 0/5; milestones 0.000; invalid 0.3 % (1/320: a `key` call with the letters of `admin` as separate keys) |
| end reasons | step budget (60) ×5; classes budget-sane 2, budget-looping 3 |
| median steps / wall / reset | 60 / 206 s / 95 s |
| $ VM | **$0.039**; 745 k tokens in / 21 k out |

**Reading:** the fastest logins of the four probes (seeds 200, 201, 204 log in on the first attempt, by step 10),
then the same wall as (a) and (b): the model is inside OpenEMR (calendar, step 10 of seed 200: "we first need to get
to the patient record for Amelia Castellano"), clicks the *Patient* menu, and within a few steps reaches for
`visit_url` with an invented address — `/openemr/patient/1`, `/openemr/claims/C-60204`,
`/openemr/index.php/patient/patient.php?username=admin&password=pass&demo=xavier-ruiz` — which drops the session
(OpenEMR's login page again, or a blank page it then waits on), and re-logs in. No Chrome crash in these five episodes
(crashpad mentions 0), so the crash is not the binding constraint here; the binding constraint is that base Fara does
not know how to navigate OpenEMR by clicking and navigates by guessed URLs instead. `openemr_chart` 0/5.

### Gate decision

| probe | logins | ask_user stops | deepest rung | $ VM |
| --- | --- | --- | --- | --- |
| (a) instruction note | **5/5** | 0 | openemr_login | 0.047 |
| (b) no-user prompt | 4/5 | 0 | openemr_login | 0.039 |
| (c) history notes | 1/5 | 0 | openemr_login | 0.047 |
| (a)+(b) | **5/5** | 0 | openemr_login | 0.039 |

Rule applied (written into `runs/logs/bakeoff_chain.sh` before the result was known): the run with the most
`openemr_login` episodes wins, ties broken by the sum of all database rungs, then by the union. (a) and (a)+(b) tie
at 5/5 and one rung; the union wins the tie and is the bake-off configuration: `--nav-macro
--system-prompt-file forkloop/policies/prompts/fara_no_user_v1.md --instruction-note "Credentials for OpenEMR: username
admin, password pass. Click the Username field, type admin, click the Password field, type pass, click Login."`. Both
are convention-level: the note restates what the instruction already says, the prompt removes a rule that has no
meaning without a user and adds the world conventions every teacher prompt carries. The login wall was reading, not
motor: the SFT data will fix it for free (every verified trajectory shows the two-field login), and the first wall
past it is OpenEMR navigation, which only SFT teaches.

## Step 2 — the milestone staircase (built before the probes, commit bf8196a)

`ClaimsOpsWorld.ui_milestones` runs after the oracle and stores, under `verdict.details["ui_milestones"]` and with no
effect on the reward, the rungs `openemr_login` (a `log` row `event LIKE 'login%'` with `success` 1 after the
watermark; failures counted alongside), `openemr_chart` (a `log` row keyed by the target patient, or an audited request
path under `patient_file`), `openemr_document` (an `http-request` row whose base64-decoded path is a document view),
`portal_claim` and `portal_appeal_form` (`page_views` paths of the target claim), `appeal_submitted` (an `appeals`
row). `scripts/milestone_staircase.py` prints the percentages per run and adds two trajectory rungs, `login_page`
(the task's username typed) and `auth_typed` (the expected authorization number typed anywhere). Three offline tests
on the fake backend (seeded audit rows on a verified and on a failed episode; the script on a recorded run with and
without the rungs). The live evidence matched the detector on the first probe: OpenEMR 8.3 logs `login` rows with
`success` 0/1 and `http-request-update` rows whose base64 comments are the request path.

**The three 2026-09-05 runs cannot be re-read with it**: their verdicts predate the rungs and the machines are gone,
so the database rungs print "needs a re-run"; only the trajectory rungs apply to them (`login_page` 30/30 in the two
`--nav-macro` runs, `auth_typed` 0/30 everywhere), which is the same picture the ledger already drew by hand.

## Step 3 — the bake-off with the winning configuration (`runs/fara15-4b-fair-f3-s200-229`, 21:07–23:25 local)

`collect --policy student --student-url http://127.0.0.1:8001/v1 --model microsoft/Fara1.5-4B --prompt-style fara
--history-k 8 --nav-macro --max-steps 120 --max-seconds 900 --retry-failed 0 --pool-mode revert --concurrency 2
--split train --families resolve_denial --seeds 200-229 --system-prompt-file
forkloop/policies/prompts/fara_no_user_v1.md --instruction-note "Credentials for OpenEMR: username admin, password
pass. Click the Username field, type admin, click the Password field, type pass, click Login."` — the same seeds,
steps and wall as the three 2026-09-05 runs. Five revert 503s ("no warm desktop hosts") were each replaced in place
and one reset was retried after 60 s; revert mode held, no fork died mid-episode, `reap --dry-run` = 0 afterwards.

| metric | value |
| --- | --- |
| success | **0/30 = 0.0 % [0.0, 11.3]**; milestones 0.000 |
| **staircase** | login_page 30/30, **openemr_login 30/30**, openemr_chart 0/30, document 0/30, auth_typed 0/30, portal_claim 0/30, appeal_form 0/30, appeal_submitted 0/30 |
| invalid-action rate | 0.0 % [0.0, 0.2] (1 of 3,643) |
| reason codes / end reasons | NOT_DONE = 30; step budget (120) = 30; `ask_user_question` = **0** (was 10/30) |
| classes (`classify_failures.py`) | budget with sane actions 20, budget with looping 10; invalid/parse, wrong-record, transcription, decoy 0 |
| median steps / wall / reset | 121 / 482 s / 52 s (reset p90 149 s, max 280 s) |
| seconds per model call (two episodes sharing the server) | median 3.78 s, p90 4.76 s (n = 2,921) |
| $ VM (`forkloop metrics`, $0.134/h) | **$0.540**; 9.47 M tokens in / 264 k out, priced at $0 |

**The histogram, past the login.** Every episode logs into OpenEMR (73 successful logins over the run, 2.4 per
episode; 151 failed ones, mostly `admin` retyped into the Password field once the fields were cleared) and every one
dies between the login and the patient chart. Per-episode table in `runs/logs/postlogin-fara15-4b-fair.md`:

- **Navigation by invented URL, 25/30 episodes (92 `visit_url` calls with made-up OpenEMR paths, 242 macro
  expansions in all).** Inside OpenEMR the model clicks the *Patient* menu once or twice, then reaches for
  `visit_url` — `/openemr/login.php`, `/openemr/index.php?route=login`, `/openemr/interface/main.php?…&pat_id=C-60206`,
  `/openemr/interface/login/login.php?username=admin&password=pass` — which lands on a 404, a blank page or the login
  page, and it logs in again. The loop is login → calendar → guessed URL → login; 17/30 also flip to the portal
  (`localhost:8080`) and back. This is the browser-harness habit the nav macro made executable: a base Fara that
  cannot navigate by clicking through OpenEMR's menus navigates by URL, and OpenEMR has no guessable URLs.
- **Chrome's post-login crash, 12/30 episodes** ("Aw, Snap! Error code: 5" on `interface/main/tabs/main.php`, then
  Reload → login page again). The prompt's reload rule is followed; the session is gone after the crash.
- **Zero `ask_user_question` stops** (the no-user prompt removed the whole class), zero wrong-record, collateral,
  duplicate, transcription or decoy failures: nothing got far enough.

**The `resolve_denial_easy` run was not made.** The gate for it was "sane actions past the login", and the actions past
the login are sane (menus, search pages, reloads) but no episode reaches the patient chart, and the easy variant only
changes the document (page 1, no distractors) — it cannot move a number that is decided two screens earlier. The
$0.45 and two hours went to the 9B scale check instead.

## Step 4 — the scale check: `microsoft/Fara1.5-9B` on the same Mac

Triggered by the rule "step 3 is 0/30 with episodes past the login" (30/30 logged in). The 4B server was stopped and
mlx-vlm 0.6.17 started with `--model microsoft/Fara1.5-9B` (bf16 safetensors from the HF cache, 18 GB on disk;
`runs/logs/serve_9b.sh`, log `runs/logs/mlx-server-fara15-9b.log`); the server process reports 7.6 GB RSS after the
first request (the 4B reported 9.0 GB — mlx's memory-mapped weights are not all counted in RSS, so the number is what
`ps` says, not a measurement of the model's footprint).

| click check (`scripts/student_click_check.py --model microsoft/Fara1.5-9B --seed 200`, `runs/logs/click-check-9b-s200`) | value |
| --- | --- |
| raw output | `left_click` at **(316, 194)** in the 1000×1000 space (the 4B said (315, 195)) |
| rescaled | **(404, 140)** → the crosshair sits on the *Patients* nav link (hand-checked) |
| seconds per call | **4.56 s** for the first call (2,521 prompt tokens, 63 generated at 31 tok/s decode; the 4B: 1.5–1.8 s) |
| reset | 88.8 s (one fork) |

Bake-off `runs/fara15-9b-fair-f3-s200-229`: the step-3 command with `--model microsoft/Fara1.5-9B`, started 23:30 local.

Bake-off `runs/fara15-9b-fair-f3-s200-229` (23:30–03:27 local): the step-3 command with `--model microsoft/Fara1.5-9B`.
Two revert 503s replaced in place, no reset retries, no fork died; `reap --dry-run` = 0 afterwards.

| metric | value |
| --- | --- |
| success | **0/30 = 0.0 % [0.0, 11.3]**; milestones 0.000 |
| **staircase** | login_page 30/30, **openemr_login 30/30**, **openemr_chart 4/30 = 13 % [5.3, 29.7]** (seeds 220, 223, 226, 228), Documents tab opened 4/30 (`evidence.openemr_documents_list`), **openemr_document 0/30**, auth_typed 0/30, portal_claim 0/30, appeal_form 0/30, appeal_submitted 0/30 |
| invalid-action rate | 0.0 % [0.0, 0.2] (1 of 3,582) |
| reason codes / end reasons | NOT_DONE = 30; step budget (120) = 24, **wall budget (900 s) = 6**; `ask_user_question` = 0 |
| classes | budget with sane actions 14, budget with looping 16; the other four classes 0 |
| median steps / wall / reset | 120 / 871 s / 93 s (reset p90 164 s, max 211 s) |
| **seconds per model call** (two episodes sharing the server) | **median 8.09 s, p90 9.95 s** (n = 2,727); 4.6 s alone on the click check — the 4B was 3.78 / 4.76 / 1.5 |
| $ VM | **$0.950**; 8.88 M tokens in / 301 k out, priced at $0 |

**Reading.** Size buys OpenEMR knowledge, not the task. The 9B logs in cleanly (88 successful logins, 13 failed —
the 4B had 73 / 151), knows the *Finder* tab and the global "Search by any demographics" box, types the surname, and
in four episodes opens the patient's chart and its *Documents* tab (screenshot `…000223…/shots/036_after.png`: the
Documents page scrolled to the upload panel, the category tree on the left collapsed). It never opens a letter: it
scrolls the upload area, clicks the same spot on the tree or the search box ten or more times (**28/30 episodes have
one click position repeated ≥ 10 times**, which is what the looping class counts), or leaves for the login checklist
again ("the task explicitly provides OpenEMR login credentials and instructs me to log in"; 27/30 episodes still
type invented `/openemr/login…` URLs, 118 in all). Chrome's post-login crash hit 19/30 episodes. Six episodes were
cut by the 900 s wall at 8 s per call, so the like-for-like comparison with the 4B is on steps only for 24/30; a
fair 9B number needs either `--max-seconds 1800` or a GPU server — noted, not run (same flags were the brief).

**The document rung was wrong the first time and is fixed.** The first detector counted any audited request path
containing "document", which matched OpenEMR's `/Documentation/help_files/*` pages (loaded by the dashboard) and the
automatic patient-picture fetch (`controller.php?document&retrieve…document_id=-1…context=patient_picture`), so the
raw 9B verdicts said `openemr_document` 4/30. `document_view_path()` now requires the documents controller with
`retrieve`/`view` and a real document id, the Documents list is recorded separately as
`evidence.openemr_documents_list`, and `milestone_staircase.py` re-derives the rung from the recorded path samples
for verdicts written before the fix (the 9B verdicts on disk keep the old flag; the script prints 0/30). The
trajectories confirm it: no 9B reasoning line ever describes a PDF or a letter on screen, and `auth_typed` is 0/30.

## Step 5 — the SFT plan

Written into `docs/HANDOFF.md` ("Next steps", item 1): the 25 / 50 / 94 / 115 family-3 ladder with the exact
`make_sft` / `train_lora.py` / `vllm serve` commands, evaluated on seeds 200–229 with exactly the step-3 flags
(`--nav-macro`, the no-user prompt, the note), so `fara15-4b-fair-f3-s200-229` = 0/30 with 30/30 logins is the
like-for-like floor and the staircase is the metric to watch before the success rate. Nothing was trained.

## Every run of the session

| run id | model | configuration | seeds | success (Wilson 95 %) | invalid rate | staircase (login → chart → document → auth typed → claim → form → appeal) | reason codes / ends | s per call (median, p90) | $ VM |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| click-check-s200-0906 | Fara1.5-4B | one click on a fork | 200 | n/a (crosshair on *Patients*) | 0/1 | — | — | 1.76 (one call) | ≈ 0.005 |
| fara15-4b-probe-a-note-f3-s200-204 | Fara1.5-4B | `--nav-macro --instruction-note`, 60 steps | 200–204 | 0/5 = 0 % [0, 43.5] | 0.0 % (0/304) | **5/5** → 0 → 0 → 0 → 0 → 0 → 0 | NOT_DONE 5; max_steps 5 | 3.7, 4.7 | 0.047 |
| fara15-4b-probe-b-prompt-f3-s200-204 | Fara1.5-4B | `--nav-macro --system-prompt-file fara_no_user_v1.md`, 60 steps | 200–204 | 0/5 = 0 % [0, 43.5] | 0.0 % (0/300) | **4/5** → 0 → 0 → 0 → 0 → 0 → 0 | NOT_DONE 5; max_steps 5; ask_user 0 | 3.7, 4.7 | 0.039 |
| fara15-4b-probe-c-notes-f3-s200-204 | Fara1.5-4B | `--nav-macro --history-notes`, 60 steps | 200–204 | 0/5 = 0 % [0, 43.5] | 0.0 % (0/301) | 1/5 → 0 → 0 → 0 → 0 → 0 → 0 | NOT_DONE 5; max_steps 5 | 3.7, 4.8 | 0.047 |
| fara15-4b-probe-ab-f3-s200-204 | Fara1.5-4B | note + prompt, 60 steps | 200–204 | 0/5 = 0 % [0, 43.5] | 0.3 % (1/320) | **5/5** → 0 → 0 → 0 → 0 → 0 → 0 | NOT_DONE 5; max_steps 5 | 3.7, 4.8 | 0.039 |
| **fara15-4b-fair-f3-s200-229** | Fara1.5-4B | note + prompt + `--nav-macro`, 120 steps / 900 s | 200–229 | **0/30 = 0 % [0, 11.3]** | 0.0 % (1/3,643) | **30/30 → 0 → 0 → 0 → 0 → 0 → 0** | NOT_DONE 30; max_steps 30; ask_user 0 | **3.78, 4.76** | **0.540** |
| click-check-9b-s200 | Fara1.5-9B | one click on a fork | 200 | n/a (crosshair on *Patients*) | 0/1 | — | — | 4.56 (one call) | ≈ 0.005 |
| **fara15-9b-fair-f3-s200-229** | Fara1.5-9B | same flags | 200–229 | **0/30 = 0 % [0, 11.3]** | 0.0 % (1/3,582) | **30/30 → 4/30 [5.3, 29.7] → 0 → 0 → 0 → 0 → 0** | NOT_DONE 30; max_steps 24, max_seconds 6; ask_user 0 | **8.09, 9.95** | **0.950** |
| **total** | | | 80 episodes + 2 clicks | **0 verified** | | | | | **$1.67** |

Token cost is $0 throughout (local models; `forkloop metrics` prices both Fara ids at zero: 29.9 M tokens in,
0.95 M out over the session).

## Spend against the budgets

| budget | limit | spent | left |
| --- | --- | --- | --- |
| Solari (VM only, $0.134/h) | $5.00 ($3.00 in the wrap-up brief) | **$1.67** (six collects + two click-check forks; snapshot storage unbilled until 2026-10-01) | $3.33 ($1.33 against $3) |
| OpenAI | $20.00 | $0.00 | $20.00 |
| Anthropic | $0 | $0.00 | — |

## Every failure class, with counts (80 episodes)

| class | probes (a, b, c, ab) | 4B fair | 9B fair | signature |
| --- | --- | --- | --- | --- |
| invalid/parse | 0 | 0 | 0 | one malformed `key` call per 30-seed run |
| budget with sane actions | 2 + 3 + 2 + 2 | 20 | 14 | logged in, then login → calendar → invented URL → login page; the 9B adds Finder / search / chart / Documents tab before the same loop |
| budget with looping | 3 + 2 + 3 + 3 | 10 | 16 | the same login-page or search-box click ≥ 5 times (9B: 28/30 episodes have one click position ≥ 10 times) |
| wrong-record / transcription / decoy | 0 | 0 | 0 | nothing reached a document |
| `ask_user_question` stop (sub-class, was 10/30) | 0 | 0 | 0 | removed by the no-user prompt; (c) with the trained prompt also 0/5 at 60 steps |
| Chrome "Aw, Snap!" after login (harness, per episode) | 3/5, 2/5, —, 0/5 | 12/30 | 19/30 | `interface/main/tabs/main.php`, error code 5, session lost; Chrome runs with `--disable-gpu` |
| **total failed** | **20** | **30** | **30** | |

## Three most useful next actions

1. **Run the SFT ladder on a rented GPU and read it with the staircase** (HANDOFF item 1, exact commands there):
   the floor is `fara15-4b-fair-f3-s200-229` = 0/30 with 30/30 logins and 0/30 charts. The first rung that moves
   `openemr_chart`, `openemr_document` and `auth_typed` off zero is the one that matters, before any success shows.
   Every behaviour that sinks the base models — one-field login, invented URLs, the 10-click loop on a focused field,
   the collapsed Documents tree — is absent from the 115 verified teacher episodes.
2. **Take the two harness losses out of the ladder's denominator**: Chrome's post-login crash (12/30 and 19/30
   episodes; golden v6 with `--disable-gpu` at Chrome's own start and `disk_gb` 20, HANDOFF item 3), and the wall
   budget for 9B-class models on this Mac (6/30 episodes cut at 8 s per call: serve the ladder's checkpoints with
   vLLM on the GPU box, or set `--max-seconds 1800` for local 9B runs and say so in the run id).
3. **Make the staircase a first-class metric**: fold `ui_milestones` into `forkloop metrics` and `train/eval.py`'s
   `eval_summary.json` (today it is a script over the verdicts), so every ladder rung reports chart / document /
   auth-typed rates next to success, and add `openemr_documents_list` as a rung between chart and document (the
   9B reached it 4/30; the tree must then be expanded, which no base model did).

Session end: the six collects above, `reap --dry-run` = 0 machines, the account at four snapshots, the model server
stopped, 210 offline tests green.
